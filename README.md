# Wazin

**Wazin** is a personal expense management application built with **.NET 9** and **SQL Server**.  
It helps users track their income, expenses, and savings efficiently.

---

## 📂 Project Structure

- **Backend (API)**: ASP.NET 9 Web API   
- **Database**: SQL Server Local  

---

## ⚙️ Features

- User registration and authentication  
- Add, edit, and delete expenses and income entries  
- Manage savings plans and budget limits  
- Dynamic and user-friendly interface  

---

## 🚀 How to Run

1. Open the project in **Visual Studio 2022** or any IDE that supports .NET 9.  
2. Update the **Connection String** in `appsettings.json` to match your SQL Server.  
3. Run the project, it will be available at `https://localhost:5001` (or your configured port).  

---

## 📌 Notes

- **Entity Framework Core** is used for database management.  
- Some folders like `obj/` and `bin/` are ignored via `.gitignore` to avoid unnecessary files.  

---

## 🛠️ Technologies Used

- C# (.NET 9)  
- ASP.NET Web API  
- SQL Server  
- Git & GitHub for version control
